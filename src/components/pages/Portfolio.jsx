"use client"

export default function Skills({ bg }) {


    return <section style={{ backgroundImage: `url("${bg}")` }}>
        <header>

            <h1 className='font-tourner text-7xl'>hero</h1>
        </header>

    </section>
}